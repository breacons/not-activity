import React from 'react';

interface WaveLengthProps {
  stream: MediaStream;
}

export const WaveLength = ({ stream }: WaveLengthProps) => {
  return <span>WaveLength</span>;
};

export default WaveLength;
